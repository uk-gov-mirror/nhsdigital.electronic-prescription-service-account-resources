{
    "name": "driftDetector",
    "version": "1.0.0",
    "description": "Lambda to check for cloudformation drift",
    "main": "driftDetector.ts",
    "author": "NHS England",
    "license": "MIT",
    "dependencies": {
        "@aws-sdk/client-cloudformation": "^3.1032.0",
        "@aws-sdk/client-cloudwatch-logs": "^3.1032.0",
        "@middy/core": "^7.2.3",
        "@middy/input-output-logger": "^7.3.2"
    },
    "scripts": {
        "compile": "tsc",
        "test": "npm run compile && npm run unit",
        "unit": "echo \"Error: no test specified\"",
        "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs ."
    },
    "devDependencies": {
        "aws-sdk-client-mock": "^4.1.0"
    }
}
