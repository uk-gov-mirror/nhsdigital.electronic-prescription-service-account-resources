{
    "name": "driftDetector",
    "version": "1.0.0",
    "description": "Lambda to check for cloudformation drift",
    "main": "driftDetector.ts",
    "author": "NHS England",
    "license": "MIT",
    "dependencies": {
        "@aws-sdk/client-cloudformation": "^3.917.0",
        "@aws-sdk/client-cloudwatch-logs": "^3.926.0",
        "@middy/core": "^6.4.5",
        "@middy/input-output-logger": "^6.4.5"
    },
    "scripts": {
        "compile": "tsc",
        "test": "npm run compile && npm run unit",
        "unit": "POWERTOOLS_DEV=true NODE_OPTIONS=--experimental-vm-modules jest --no-cache --coverage",
        "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs ."
    },
    "devDependencies": {
        "aws-sdk-client-mock": "^4.1.0"
    }
}
