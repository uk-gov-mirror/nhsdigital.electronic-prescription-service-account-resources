{
  "name": "splunkProcessor",
  "version": "1.0.0",
  "description": "Processor lambda for converting cloudwatch logs to splunk",
  "main": "splunkProcessor.js",
  "author": "NHS Digital",
  "license": "MIT",
  "scripts": {
    "test": "POWERTOOLS_DEV=true jest --coverage",
    "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs .",
    "check-licenses": "license-checker --failOn GPL --failOn LGPL --start ../.."
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.28.1",
    "@aws-sdk/client-firehose": "^3.926.0",
    "@aws-sdk/client-kinesis": "^3.917.0",
    "aws-sdk": "^2.1692.0"
  }
}
