{
  "name": "splunkProcessor",
  "version": "1.0.0",
  "description": "Processor lambda for converting cloudwatch logs to splunk",
  "main": "splunkProcessor.js",
  "author": "NHS Digital",
  "license": "MIT",
  "scripts": {
    "test": "npm run unit",
    "unit": "POWERTOOLS_DEV=true vitest run --config ./vitest.config.ts --coverage",
    "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs ."
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.33.0",
    "@aws-sdk/client-firehose": "^3.1032.0",
    "@aws-sdk/client-kinesis": "^3.1032.0"
  },
  "devDependencies": {
    "aws-sdk-vitest-mock": "^1.0.55",
    "vitest": "^4.1.5"
  }
}
